export default interface IFileuploaderProps {
  description: string;
  target_library: string;
  required_fields: Array<any>;
  required_fields_metadata: Array<any>;
  required_fields_schema: Array<any>;
  header_list: Array<any>;
}
